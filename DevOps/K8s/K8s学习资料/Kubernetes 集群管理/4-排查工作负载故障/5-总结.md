# 总结

在本实验中，你学习了排查 Kubernetes 故障的基本工作流：

1. **观察 (Observe)**：通过 `kubectl get pods` 识别故障工作负载。
2. **检查 (Inspect)**：使用 `kubectl describe pod` 查看事件日志并查找错误消息。
3. **修复 (Fix)**：更新 YAML 清单并应用修正。

这个「获取、描述、修复」的过程将解决你在 Kubernetes 生涯中遇到的 90% 的部署问题。
