# 总结

在本实验中，你练习了 Kubernetes 节点维护的安全生命周期。你学习了如何：

1. **Cordon**（封锁）节点以将其标记为不可调度（请勿打扰）。
2. **Drain**（驱逐）节点以安全地移除正在运行的工作负载。
3. **Uncordon**（解除封锁）节点以将其恢复服务。

这些命令对于系统管理员确保高可用性至关重要。它们允许你在不导致应用程序停机的情况下修复基础设施（当然，是在多节点集群中！）。
