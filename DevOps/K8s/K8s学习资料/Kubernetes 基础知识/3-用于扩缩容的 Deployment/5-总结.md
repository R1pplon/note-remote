# 总结

在本实验中，你了解了 **Deployment**，这是一个让你的应用程序保持运行的智能管理器。

你成功完成了以下操作：

1. 创建了一个包含 3 个工作者 Pod 的 Deployment。
2. 验证了 Deployment 会自动替换故障 Pod（**自我修复**）。
3. 通过单个命令对副本数量进行了扩缩容（**扩缩容**）。

现在你明白了为什么我们很少创建独立的 Pod。Deployment 为你提供了可靠性和灵活性，它处理了复杂的调度和重启工作，让你无需操心。
